# Antigravity Custom Rules

## Workflow Constraints
- **Branch-only Development**: Always perform coding tasks, file modifications, and changes inside a new dedicated Git branch (never directly on `main` or `staging`). Leave merging to the user unless explicitly instructed otherwise.
